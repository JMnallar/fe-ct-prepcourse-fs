function multiplica(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su multiplicación.
  // Tu código:
    // Paso 1: Realizar la suma de los dos números recibidos como argumentos
    let resultado = x * y;

    // Paso 2: Retornar el resultado de la suma
    return resultado;
}

module.exports = multiplica;